CREATE INDEX idx_OrderID
ON Orders (OrderID);


-- question -- Explain one benefit of creating an index
-- An index speeds up data retrieval by helping the database locate records quickly 
-- instead of scanning the entire table.
